class Heap {
    constructor(arr = []) {
        this.heap = []
        if (arr){
            this.heap = heapify(arr);
        }
    }

    peek() {
        return this.heap[0];
    }

    heapPush(val) {
        
    }

    heapPop(val) {

    }

    heapPushPop(val) {

    }

    heapify(arr) {
        
    }
}

export default Heap